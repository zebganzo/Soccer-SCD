with Soccer.Root_Event;
use Soccer.Root_Event;
with Soccer.Root_Event.Motion_Root_Event;
with Soccer.Root_Event.Game_Root_Event;

package Soccer.Bridge is

--     type EventArray is array (1 .. 3) of Event_Ptr;

   procedure Print_All (E : Event_Ptr);

end Soccer.Bridge;
